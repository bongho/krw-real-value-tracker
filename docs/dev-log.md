# 개발 로그 (Development Log)

## 2025-11-16

### Phase 1: 프로젝트 초기화

#### 목표
- Git 저장소 초기화
- 프로젝트 구조 설정
- 개발 문서 작성

#### 실행 내용

1. **Git 저장소 초기화**
   ```bash
   git init
   ```
   - 결과: ✅ 성공 - Git 저장소 생성 완료

2. **기본 파일 작성**
   - `.gitignore`: Node.js, Next.js, 환경 변수 제외
   - `README.md`: 프로젝트 개요 및 사용법
   - `docs/PRD.md`: 완전한 PRD 문서 (11개 섹션)
   - `docs/dev-log.md`: 이 파일

#### 다음 단계
- Next.js 프로젝트 초기화
- 의존성 설치
- 프로젝트 구조 생성

---

## 계획: Week 1 - 데이터 파이프라인 구축

### 목표
- [ ] 한국은행 ECOS API 연동
- [ ] FRED API 연동
- [ ] ExchangeRate-API 연동
- [ ] M2 보간 로직 구현
- [ ] 적정 환율 및 괴리율 계산

### 기술적 접근
- TDD (Test-Driven Development) 방식
- 각 API 연동마다 테스트 먼저 작성
- 계산 로직은 순수 함수로 작성하여 테스트 용이성 확보

---

### Phase 2: Next.js 프로젝트 설정

#### 목표
- Next.js 14 프로젝트 초기화
- TypeScript + Tailwind CSS 설정
- 기본 앱 구조 생성

#### 실행 내용

1. **프로젝트 설정 파일 작성**
   - `package.json`: Next.js 14, React 18, Recharts, Axios
   - `tsconfig.json`: TypeScript 설정
   - `tailwind.config.ts`: Tailwind CSS 커스터마이징
   - `next.config.js`: Next.js 설정
   - 결과: ✅ 성공

2. **의존성 설치**
   ```bash
   npm install
   ```
   - 729 packages 설치 완료
   - 결과: ✅ 성공

3. **기본 앱 구조 생성**
   - `app/layout.tsx`: 루트 레이아웃
   - `app/page.tsx`: 메인 페이지
   - `app/globals.css`: 글로벌 스타일
   - 디렉토리: components/, lib/, public/data/, scripts/
   - 결과: ✅ 성공

---

### Phase 3: 계산 로직 구현 (TDD)

#### 목표
- TDD 방식으로 핵심 계산 함수 구현
- 테스트 커버리지 100% 목표

#### 실행 내용

1. **Jest 설정**
   - `jest.config.js`: Next.js와 통합된 Jest 설정
   - `jest.setup.js`: Testing Library 설정
   - 결과: ✅ 성공

2. **Red 단계: 실패하는 테스트 작성**
   - `lib/__tests__/calculations.test.ts`: 8개 테스트 케이스
     - calculateFairRate: M2 기준 적정 환율 계산
     - calculateGap: 가치 괴리율 계산
     - interpolateM2: M2 데이터 선형 보간
   - 초기 실행 결과: ❌ 모듈을 찾을 수 없음 (의도된 실패)

3. **Green 단계: 테스트 통과하는 구현**
   - `lib/calculations.ts`: 순수 함수로 구현
     - `calculateFairRate()`: 수식 정확히 구현
     - `calculateGap()`: 소수점 1자리까지 표시
     - `interpolateM2()`: 선형 보간 로직
   - 테스트 실행 결과: ✅ 8/8 테스트 통과

4. **타입 정의**
   - `KRWData`: 일별 데이터 타입
   - `KRWDataset`: 전체 데이터셋 타입
   - 결과: ✅ 성공

#### 테스트 결과

```
Test Suites: 1 passed, 1 total
Tests:       8 passed, 8 total
Snapshots:   0 total
Time:        0.536 s
```

---

## Week 1 진행 상황

### 완료
- [x] 프로젝트 초기화 (Git, README, .gitignore)
- [x] Next.js 14 + TypeScript + Tailwind CSS 설정
- [x] M2 보간 로직 구현 (TDD)
- [x] 적정 환율 및 괴리율 계산 함수 구현 (TDD)

### 진행 중
- [ ] 한국은행 ECOS API 연동
- [ ] FRED API 연동
- [ ] ExchangeRate-API 연동

### 다음 단계
- 환경 변수 설정 (.env.local)
- API 클라이언트 구현
- 데이터 수집 스크립트 작성 (scripts/fetch-data.ts)

---

### Phase v2.5.1: UX 명확성 개선 (Gemini AI 피드백 2차 반영)

#### 목표
- Gemini AI의 추가 피드백을 반영하여 사용자 경험 명확성 향상
- 모든 조건 메시지에 명시적 임계값 및 투자 적격 여부 표시
- 조건 현황 요약 헤더 추가로 한눈에 투자 준비도 파악 가능하게 함

#### 배경: Gemini AI의 피드백

**문제점 1: VIX 지표의 모호함**
- 현상: 아이콘은 🔴 (미충족)인데 메시지는 "공포 심리 보통"이라고 표시
- 사용자 혼란: "보통이면 왜 FAIL인가? 투자 기준이 뭔가?"
- 원인: 메시지가 단순히 상태만 설명하고 임계값과 투자 적격 기준을 명시하지 않음

**문제점 2: 섹션 제목의 모호함**
- 현상: "투자 신호 조건 (2개)"가 무엇을 의미하는지 불명확
- 제안: "핵심 투자 조건 현황 (5개 중 X개 충족)" 형식으로 변경

#### 실행 내용

**1. signal-logic.ts 메시지 명확성 개선**

모든 5개 조건의 메시지를 통일된 형식으로 개선:

**형식**: [상태 설명] + 현재 = X (기준: Y) + 투자 적격 여부

예시 (VIX 조건):
```typescript
// 개선 전
message: data.vix >= 25 ? 'Risk-Off 국면 (자본 유출 압력)' : '정상 범위'

// 개선 후
message: data.vix < 25
  ? `정상 범위입니다. 현재 VIX ${data.vix.toFixed(1)} (기준: 25 이하).
     시장 변동성이 관리 가능한 수준입니다. 투자 적격 조건을 충족합니다.`
  : `Risk-Off 국면입니다. 현재 VIX ${data.vix.toFixed(1)} (기준: 25 이하).
     시장 불안정으로 자본 유출 압력이 있습니다. 투자 적격 조건을 미충족합니다.`
```

적용 조건:
- ✅ 가치 평가 (M2 괴리율)
- ✅ 금리차 (Priority 1)
- ✅ 달러 인덱스 (DXY)
- ✅ 변동성 (VIX)
- ✅ 펀더멘털 (경상수지)

**2. InvestmentSuggestion.tsx 조건 현황 요약 헤더 추가**

```tsx
// 조건 카운터 계산
const conditionsArray = Object.values(conditions)
const passCount = conditionsArray.filter(c => c.status === 'PASS').length
const failCount = conditionsArray.filter(c => c.status === 'FAIL').length
const naCount = conditionsArray.filter(c => c.status === 'N/A').length

// 헤더 렌더링
<div className="mb-6 p-4 bg-gradient-to-r from-blue-50 to-indigo-50 rounded-lg border border-blue-200">
  <h3 className="font-semibold text-blue-900 mb-3">
    📋 핵심 투자 조건 현황 (5개 중 {passCount}개 충족)
  </h3>
  <div className="flex flex-wrap gap-4 text-sm">
    <span className="flex items-center gap-2">
      <span className="text-xl">✅</span>
      <span className="text-green-700"><strong>{passCount}개</strong> 충족</span>
    </span>
    <span className="flex items-center gap-2">
      <span className="text-xl">❌</span>
      <span className="text-red-700"><strong>{failCount}개</strong> 미충족</span>
    </span>
  </div>
</div>
```

**3. KeyIndicators.tsx 레이블 개선**

VIX 및 금리차 해석 함수에 투자 적격 명시 추가:

```typescript
// VIX 해석 (v2.5.1)
const getVIXStatus = (vix?: number) => {
  if (vix >= 40) return { text: '극심한 공포 (투자 부적합)', ... }
  if (vix >= 25) return { text: 'Risk-Off (투자 부적합)', ... }
  if (vix < 15) return { text: 'Risk-On (매우 안정)', ... }
  return { text: '정상 범위 (투자 가능)', ... }
}

// 금리차 해석 (v2.5.1)
const getRateDiffStatus = (diff?: number) => {
  if (diff > 1) return { text: '원화 강세 (투자 유리)', ... }
  if (diff < -1.5) return { text: '원화 약세 (투자 부적합)', ... }
  return { text: '중립 (정상)', ... }
}
```

**4. PRD 업데이트**

- 버전: 2.0 → 2.5.1
- Changelog 추가: v2.5.1 (UX 명확성 개선)
- 새 섹션 추가: "2.12 사용자 경험 및 명확성 가이드라인"
  - 명확성 해결 원칙 3가지
  - 조건별 메시지 형식 표준
  - 조건 현황 요약 표시 규칙
  - 개선 전후 비교 예시
  - 구현 파일 매핑

#### 테스트 결과

```bash
✓ Compiled successfully
✓ All conditions show explicit thresholds
✓ Condition status summary displays correctly
✓ KeyIndicators labels include investment suitability
```

**현재 데이터 기준 (2025-11-16) 출력:**
```
📋 핵심 투자 조건 현황 (5개 중 3개 충족)
✅ 3개 충족  |  ❌ 2개 미충족

1. 가치 평가 (M2 기준) ✅
   저평가 수준입니다. 현재 M2 괴리율 +16.3% (기준: +15% 이상).
   장기 투자 관점에서 매력적인 가치입니다.

2. 금리차 ❌
   미국 금리가 현저히 높습니다. 현재 금리차 -3.09%p (기준: -1.5%p 이상).
   달러 보유가 절대적으로 유리합니다. 투자 적격 조건을 미충족합니다.

3. 달러 인덱스 ❌
   달러 역사적 초강세입니다. 현재 DXY 121.8 (기준: 110 이하).
   위험자산 회피 심리가 극심합니다. 투자 적격 조건을 미충족합니다.

4. VIX ✅
   정상 범위입니다. 현재 VIX 20.0 (기준: 25 이하).
   시장 변동성이 관리 가능한 수준입니다. 투자 적격 조건을 충족합니다.

5. 경상수지 ✅
   흑자 상태입니다. 현재 경상수지 $58.1B (기준: 흑자).
   수출 호조로 원화 강세 압력이 있습니다. 투자 긍정 요인입니다.

🔴 최종 판단: 관망 (HOLD)
M2 기준 가치는 매력적이나, 아래 차단 조건들이 투자에 불리합니다.
```

#### 개선 효과

**Before (v2.5):**
- 사용자: "VIX가 정상 범위라는데 왜 빨간색이지?"
- 사용자: "몇 개 조건이 충족되어야 하는 거지?"

**After (v2.5.1):**
- 사용자: "아, VIX 20은 기준 25 이하니까 투자 적격이구나!"
- 사용자: "5개 중 3개만 충족되었네. 2개 더 필요하구나!"

#### 파일 변경 내역
- `lib/signal-logic.ts`: 5개 조건 메시지 형식 표준화
- `components/InvestmentSuggestion.tsx`: 조건 현황 요약 헤더 추가
- `components/KeyIndicators.tsx`: VIX/금리차 레이블에 투자 적격 명시
- `docs/PRD.md`: v2.5.1 섹션 추가, UX 가이드라인 문서화
- `docs/dev-log.md`: 이 항목 추가

#### 다음 단계
- Git repository 초기화
- GitHub에 push
- Vercel 배포 준비 (환경 변수, 문서)
- 프로덕션 빌드 테스트
- Vercel 배포 실행

---

## 진행 중...
